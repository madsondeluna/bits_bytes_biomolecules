# Configuração de Email (SMTP)
# Para usar Gmail, você precisa:
# 1. Habilitar autenticação de dois fatores
# 2. Gerar uma "Senha de App" em https://myaccount.google.com/apppasswords

SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=seu_email@gmail.com
SMTP_PASSWORD=sua_senha_de_app_aqui

# Email do remetente (geralmente o mesmo que SMTP_USERNAME)
FROM_EMAIL=seu_email@gmail.com
FROM_NAME=Sistema de Autenticação

# Email do administrador (para receber notificações)
ADMIN_EMAIL=admin@example.com

# Habilitar/desabilitar envio de emails
EMAIL_ENABLED=false

# URL base para página de redefinição de senha
RESET_PASSWORD_URL=http://localhost:8000/reset-password.html

# DESENVOLVIMENTO: Criar usuários padrão (admin, user, researcher)
# NUNCA configure como 'true' em produção!
CREATE_DEFAULT_USERS=false
